# Phase 2 未完成开发清单（明日续做）

日期：2026-02-21（更新）

## 本轮已完成（代码 + 单测）

- M01 高级能力（阶段性）：
  - RolePermission / DataScopePolicy / SessionSecurityEvent
  - `requirePermission` / `requireDataScope` 中间件
  - 会话指纹漂移记录（登录风险事件）
- M03 高级能力：
  - 订单状态机 + 版本冲突检测
  - 订单冲突事件中心 + 人工解决接口
- M04 高级能力：
  - 渠道接单签名校验、每分钟配额限流
  - 死信队列（DLQ）+ 回放/丢弃 + 使用量查询
  - 治理统计增强：失败分类统计、重试窗口、DLQ Insights
- M06 高级能力：
  - 库存状态变更触发渠道可用性同步任务
  - 同步任务列表与状态流转（PENDING/SYNCED/FAILED）
- M18 合作方对接平台高级能力：
  - 合作方 API Key（scope/IP/rate-limit/sandbox）
  - 公共订单读取 API（x-api-key 鉴权）
  - Webhook 签名预览工具
  - 合作方 Webhook 投递器（事件入队、重试调度、失败追踪、手动派发）
- M20 高级能力（阶段性）：
  - 高风险审批策略与审批请求流
  - 合规策略包（country/version）管理
  - 关键动作强绑定高风险审批（退款执行/敏感导出/组织与渠道配置变更）
  - 策略包自动执行器（按国家自动绑定到门店）
- M01 高级能力（补齐）：
  - 核心路由权限迁移（`requirePermission` 全覆盖核心模块）
  - 兼容旧角色能力的权限回退矩阵（避免迁移期角色行为回退）
  - 字段级授权（FieldAccessPolicy，可配置字段读写与字段脱敏）

## 未完成（下一批重点）

### P0
- [x] M01：字段级授权（按角色/资源字段可见性与写入控制）

### P1
- [x] M04：渠道连接治理面板（重试窗口、失败分类统计）
- [x] M18：合作方 webhook 投递器（签名 + 重试 + 失败追踪）

## 约束记录
- [x] 告警外发继续暂停（钉钉/Slack/邮件不启用）
- [x] 值班升级规则已开启并继续沿用（L1/L2/L3）

## 回归状态
- [x] 后端单元测试全量通过（`npm test`，91/91）
